export interface StudyData {
    type: string;
    title: string;
    description: string;
    maxSize: number;
    startDate: string;
    endDate: string;
    techStack: string[];
    viewCount: number;
    studyStatus: string;
    writer: string;
}